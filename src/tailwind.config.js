/** @type {import('tailwindcss').Config} */

const config = {
  content: ["./**/*.html", "./static/js/*.js"],
  darkMode: "selector",
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;
